<div class="avatar-participant <%= avatar_colored(seat[:seat_id]) %>" data-path="<%= seating_plan_participant_swap_url(@seating_plan, seat[:participant_id])%>" data-pid="<%= seat[:participant_id] %>">
  <p><%= seat[:initials] %></p>
  <div class= "<%= avatar_family(seat[:seat_id]) %>" ></div>
</div>
